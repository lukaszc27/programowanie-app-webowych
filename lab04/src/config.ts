/**
 * konfiguracja aplikacji
 * w tym miejscu należy wskazać czy aplikacja
 * ma kożystać z localStorage czy z zewnętrznej
 * bazy danych Firebase
 */
export const Config = {
    UseFirebase: true
};